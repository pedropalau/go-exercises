package main

type array struct {
	// The `length` of the array.
	// Represent the total items the array have.
	count int

	// The elements in the array
	// @TODO: make this a type-dynamic array
	items int
}
